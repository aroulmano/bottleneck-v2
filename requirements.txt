# Environnement figé — projet BottleNeck v2
#
# Versions relevées et vérifiées le 24/08/2026 sur Python 3.11.
# Reproduire l'environnement :
#   python -m venv .venv
#   source .venv/bin/activate        (Windows : .venv\Scripts\activate)
#   pip install -r requirements.txt
#
# Les versions sont épinglées à l'exact (==) et non à l'approché (>=) : le P6 a livré un
# résultat non reproductible, l'objet de ce fichier est que cela ne se reproduise pas.

# --- Traitement des données
pandas==3.0.2
numpy==2.4.4
openpyxl==3.1.5

# --- Validation de schéma — retenu en veille, axe 1
# Écarté : great_expectations (37 dépendances, 395 Mo, non maintenable par le client)
pandera==0.32.1

# --- Détection d'anomalies — comparaison des méthodes, axe 2
scikit-learn==1.8.0
scipy==1.17.1

# --- Visualisation
plotly==6.9.0
matplotlib==3.10.9
seaborn==0.13.2

# --- Veille automatisée, §7 du document de veille
feedparser==6.0.14

# --- Qualité de code et tests
pytest==9.1.1
ruff==0.16.4

# --- Volontairement absents
# polars / duckdb : gain absolu de 0,45 s sur 825 lignes, hors de proportion avec le coût
#                   de réécriture et de formation. Condition de réexamen consignée au
#                   registre des risques : volume > 5 M lignes ou pipeline > 30 s.
# papermill       : redondant avec l'extraction de la logique dans src/.
# jupytext        : sans objet en travail solo.
