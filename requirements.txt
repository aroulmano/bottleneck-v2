# ══════════════════════════════════════════════════════════════════════════════
# Environnement de référence — projet BottleNeck v2
#
# Versions exactes sous lesquelles les résultats du notebook ont été produits,
# le 24/08/2026, sous CPython 3.11.15.
#
#     python -m venv .venv
#     .venv\Scripts\activate          (Windows)
#     source .venv/bin/activate       (macOS / Linux)
#     pip install -r requirements.txt
#
# Si pip ne trouve pas de distribution pour votre version de Python — le cas sur
# les versions très récentes, pour lesquelles les paquets scientifiques publient
# leurs wheels avec plusieurs mois de décalage — utilisez requirements-souple.txt.
# Les chiffres peuvent alors différer à la marge ; le cas échéant, c'est ce
# fichier-ci qui fait foi.
# ══════════════════════════════════════════════════════════════════════════════

# ---- Traitement des données
pandas==3.0.2
numpy==2.4.4
openpyxl==3.1.5          # lecture des .xlsx sources

# ---- Validation de schéma — retenu en veille, axe 1
pandera==0.32.1

# ---- Détection d'anomalies — comparaison des méthodes, axe 2
scikit-learn==1.8.0
scipy==1.17.1            # tests de normalité, diagramme quantile-quantile

# ---- Visualisation
matplotlib==3.10.9
# Ni seaborn ni plotly : le P6 les utilisait, la version 2 n'en a plus besoin.
# Retirer une dépendance inutilisée est le levier de sobriété le plus direct
# du projet — cf. veille/veille_technologique.md §6.

# ---- Veille automatisée, axe 3
feedparser==6.0.14

# ---- Tests et génération du notebook
pytest==9.1.1
nbformat==5.11.1
nbconvert==7.17.1
ipykernel==7.3.0
